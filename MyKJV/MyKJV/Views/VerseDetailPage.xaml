<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MyKJV.Views.VerseDetailPage"
             Title="{Binding Title}">

    <StackLayout Spacing="20" Padding="15"> 
        <Label Text="{Binding Verse.FullTitle}" d:Text="Item name" FontSize="Medium"/> 
        <Label Text="{Binding Verse.Text}" d:Text="Item description" FontSize="Small"/>
        <Label Text="Memorized"   FontSize="Small"/>
        <CheckBox IsChecked="{Binding IsMemorized}" CheckedChanged="CheckBox_CheckedChanged"></CheckBox>
    </StackLayout>
    
</ContentPage>